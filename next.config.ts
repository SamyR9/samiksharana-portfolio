import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  reactStrictMode: true,
  basePath: "/samiksharana-portfolio/public",
  output: "export"
};

export default nextConfig;
